program z22;
var n, fac, i:integer;
    x, kv, sum:real;
begin
  read(x,n);
  sum:=1;
  kv:=1;
  fac:=1;
  for i:=1 to n do begin    
    kv:=kv*x;
    fac:=fac*i;
    sum:=sum+kv/fac;
  end;
  writeln(sum);
  write(exp(x));
end.
