program z23;
var i, n:integer;
    x, kv, sum, fac:real;
begin
  read(x,n);
  sum:=x;
  kv:=x;
  fac:=1;
  for i:=1 to n do begin
    kv:=kv*x*(-x);
    fac:=fac*2*i*(2*i+1);
    sum:=sum+kv/fac; 
  end;
  writeln(sum);
  write(sin(x));
end.
