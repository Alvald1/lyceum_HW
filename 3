program z24;
var i, n:integer;
    x, kv, sum, fac:real;
begin
  read(x,n);
  sum:=0;
  kv:=1;
  fac:=1;
  for i:=1 to n do begin
    sum:=sum+kv/fac; 
    kv:=kv*x*(-x);
    fac:=fac*2*i*(2*i-1);    
  end;
  writeln(sum);
  write(cos(x));
end.
